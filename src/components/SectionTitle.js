import React from 'react'

const SectionTitle = ({title}) => {
    return (
        <h4 className="text-center text-xl font-extrabold mb-12 underline md:mb-8">{title}</h4>
    )
}

export default SectionTitle
